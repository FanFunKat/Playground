doctype html
html(lang="en")
  head
    meta(charset="UTF-8")
    meta(name="viewport", content="width=device-width, initial-scale=1.0")
    title Flash Cards
  body
    header
      h1 Flash Cards
    section#content
      ul
        each color in colors
          li= color
      h2= prompt
      if hint
        p
          i Hint: #{hint}
      //- else
      //-   p (There is no hint for this card.)
    footer
      p An app to hepl you study.